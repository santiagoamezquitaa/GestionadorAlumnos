import { MaakeStatusPipe } from './maake-status.pipe';

describe('MaakeStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new MaakeStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
